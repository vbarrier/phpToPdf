<div class="signature">
    <p>
        L’organisme de formation<br/>
        Le <span class="dynamic-data">{{ pageData.signatureDate }}</span><br/>
        <span class="dynamic-data">{{ pageData.ofUserName }}</span>
    </p>

    <p>
        Cachet Signature
    </p>
</div>
