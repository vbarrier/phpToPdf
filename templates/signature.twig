<div class="signature">
    <p>
        L’organisme de formation<br/>
        Le <span class="dynamic">{{ signatureDate }}</span><br/>
        <span class="dynamic">{{ ofUserName }}</span>
    </p>

    <p>
        Cachet Signature
    </p>
</div>
